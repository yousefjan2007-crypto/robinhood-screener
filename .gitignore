cache/
config.local.json
*.out.log
*.err.log
__pycache__/
*.pyc
*.tmp
# the paper book (selfimprove/livebook.py --tick inside .github/keeper.sh): livebook.json,
# livebook_fills.csv, livebook_feed.json and livebook_missed.jsonl are COMMITTED state riding the
# scan's `git add data/` — never tick or hand-edit them on the Mac (the tree would diverge from
# origin/main and the Sunday ff-merge refuses). Only the per-tick log stays out of git: it grows
# ~3.5 MB/day and rides the keeper run's artifact instead. The weekly summary IS committed.
data/livebook_ticks.jsonl
# research-session scratch
selfimprove/research/context/
selfimprove/research/logs/
selfimprove/PAUSE
# archived proven-dev material that is held offline (see data/archive/proven_dev/README.md)
data/archive/proven_dev/social_verdicts.json
data/archive/proven_dev/mizukara_report.md
data/archive/proven_dev/history.bundle
# livebook backups (Mac-local; run_improve.sh took them until the gates moved to weekly.yml)
data/backups/
# the keeper's flock file (.github/keeper.sh, runner-only); keeper_handoff.json IS committed
data/.keeper.lock
